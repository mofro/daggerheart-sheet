import type MiniSheetPlugin from "../main";

/**
 * Phase 1 stub — MCP bridge rewritten for Daggerheart in Phase 2.
 * The Phase 0 PF1e bridge imported spell, maneuver, and buff actions
 * that no longer exist. Exports only what main.ts requires.
 */
export function installBridge(_plugin: MiniSheetPlugin): void {}
export function removeBridge(): void {}
