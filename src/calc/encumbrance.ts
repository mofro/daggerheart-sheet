/**
 * Phase 1 stub — PF1e encumbrance calc removed.
 * Daggerheart uses CarriedItem weight but no encumbrance thresholds;
 * this stub keeps InventoryPanel's type import compiling.
 */

/** @deprecated PF1e — not used in Daggerheart. */
export type EncumbranceComputed = Record<string, never>;
