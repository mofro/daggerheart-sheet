/** Phase 1 stub — PF1e modifier editor, not applicable to Daggerheart. */
export function ModifierEditor(_props: unknown): null { return null; }

/** @deprecated Phase 1 stub — imported by QAEditor effect-row. */
export const TARGET_GROUPS: unknown[] = [];
