/** Phase 1 stub — Daggerheart gear panel in Phase 2. */
export function InventoryPanel(_props: unknown): null { return null; }

/** @deprecated Phase 1 stub type for ItemRow/InventoryList importers. */
export type DisplayRow = Record<string, unknown>;
